# سند آنبوردینگ ایجنت هوش مصنوعی (Agent Onboarding)

این سند حاوی دستورالعمل‌های حیاتی برای ایجنت‌های هوش مصنوعی (AI Agents) است که روی این پروژه کار می‌کنند.

## قوانین کلی
1. **زبان ارتباطی:** تمامی پیام‌ها و توضیحات رو به کاربر، باید به زبان فارسی صمیمانه و حرفه‌ای ارائه شود.
2. **کدنویسی:** رعایت استانداردهای TypeScript الزامی است. از `any` تا حد امکان پرهیز شود.
3. **تصاویر:** مطلقاً از تصاویر با لینک خارجی (مانند Unsplash) استفاده نشود. تمامی عکس‌ها باید دانلود/تولید شده و در پوشه `public/images/` ذخیره و از آنجا فراخوانی شوند.
4. **ظاهر و UI:** سبک پروژه بر اساس افکت‌های Glassmorphism (شیشه‌ای)، کپسولی و لوکس (Premium) بنا شده است. در صورت اضافه کردن کامپوننت جدید، از سایه‌های نرم (`shadow-lg`, `shadow-xl`)، حاشیه‌های سفیدِ نیمه‌شفاف (`border-white/50`) و بک‌گراند بلور (`backdrop-blur`) استفاده شود.
5. **ارتباط با ادمین:** تغییرات ظاهری سایت باید قابلیت ویرایش از طریق پنل `src/admin/` را داشته باشند.
